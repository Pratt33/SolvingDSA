#include <bits/stdc++.h>
using namespace std;

int main() {
    // A data structure contains elements of the same type.

    // When an array is declared locally, its elements contain garbage values.
    // Example: int arr[6]; (inside main) → garbage values.

    // When declared globally, the elements are initialized to zero by default.

    // Maximum size for global array declaration: 10^7 elements.
    // Maximum size for local array declaration: 10^6 elements.

    return 0;
}