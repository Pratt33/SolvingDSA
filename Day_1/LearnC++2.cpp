#include <bits/stdc++.h>
using namespace std;

int main() {
    // Demonstrating string input with and without getline
    /*
    string s1, s2;
    cin >> s1 >> s2; // Input two strings separated by space
    cout << s1 << " " << s2;
    */

    /*
    string str;
    getline(cin, str); // Input an entire line, including spaces
    cout << str;
    */

    // Demonstrating character input
    char ch, ch1 = 'c';
    cin >> ch; // Input a single character
    cout << ch << "\n" << ch1;

    return 0;
}